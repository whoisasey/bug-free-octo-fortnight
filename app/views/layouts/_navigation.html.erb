<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "Alpha Blog", root_path, class: 'navbar-brand', id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "View Users", users_path, class: "nav-link active", 'aria-current': 'page' %>
        </li>
        <li class="nav-item">
          <%= link_to "View Articles", articles_path, class: "nav-link active", 'aria-current': 'page' %>
        </li>
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to "Create New Article", new_article_path, class: "nav-link active", 'aria-current': 'page' %>
          </li>
        <% end %>
        <% if logged_in? %>
          <li class="nav-item">
              <%= link_to current_user.username, user_path(current_user), class:'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to "Log Out", logout_path, class: "nav-link", 'aria-current': 'page', data: {"turbo-method": :delete} %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log In", login_path, class: "nav-link ", 'aria-current': 'page' %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", signup_path, class: "nav-link ", 'aria-current': 'page' %>
          </li>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>